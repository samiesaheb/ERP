[build]
builder = "nixpacks"
buildCommand = "bash build.sh"

[deploy]
startCommand = "./skyhigh-api"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
